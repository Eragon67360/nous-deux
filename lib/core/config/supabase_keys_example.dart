// Copy this file to supabase_keys.dart and fill in your project values.
// Only the PUBLISHABLE (anon) key belongs here. Never put the service_role (secret) key in the app.
//
// Supabase keys:
// - Project URL      → Supabase Dashboard → Project Settings → API → Project URL
// - anon (public)   → Project Settings → API → Project API keys → anon public  ← use this
// - service_role    → same page, service_role key → NEVER use in Flutter (server/Edge Functions only)

const String supabaseUrlPlaceholder = 'https://YOUR_PROJECT_REF.supabase.co';
const String supabaseAnonKeyPlaceholder = 'YOUR_ANON_PUBLISHABLE_KEY';

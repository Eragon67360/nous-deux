// Mapbox access token for optional 3D map (e.g. mapbox_maps_flutter).
// Not used when the app uses flutter_map (default). For Mapbox SDK, set token via
// --dart-define=MAPBOX_ACCESS_TOKEN=... or set mapboxAccessTokenLocal below.
// See docs/MAPBOX_KEYS.md.

const String mapboxAccessTokenLocal = '';

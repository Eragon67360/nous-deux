// Stub used when building with --dart-define=USE_SUPABASE_FROM_ENV=true (CI/production).
// Supabase URL/key are then taken from AppConstants (--dart-define), so these values are unused.

const String supabaseUrlLocal = '';
const String supabaseAnonKeyLocal = '';

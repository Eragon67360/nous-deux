# URLs or URL patterns listed here are not checked (manual verification only).
# One pattern per line; lines starting with # are ignored.
# The script also has built-in allowlist patterns (instagram.com, ameli.fr).
#
# Add a domain or path pattern if it reliably returns 403 or blocks CI bots,
# for example:

instagram.com
nhsinform.scot
unicef.org
plannedparenthood.org

# ameli.fr
